using Microsoft.VisualStudio.TestTools.UnitTesting;
using System;
using System.Collections.Generic;
using System.Text;

namespace FocusMark.SDK.Account
{
    [TestClass]
    public class LoginResponseTests
    {
        [TestMethod]
        public void Constructor_SetsValues()
        {
            //// Arrange
            //var tokens = new JwtTokens { AccessToken = "invalid_access", IdToken = "invalid_id" };

            //// Act
            //var response = new LoginResponse(tokens);

            //// Assert
            //Assert.AreEqual(tokens, response.JwtTokens);
        }
    }
}
